import {describe,expect,it} from 'vitest';import {dayNumber,normalize,score,scoreMarks,statistics} from './game';import {WORDS,ANSWERS,hintFor,isWord} from './corpus';
describe('motor',()=>{it('normaliza acentos e cedilha',()=>expect(normalize('Braço')).toBe('BRACO'));it('pontua duplicatas em duas passagens',()=>expect(score('ARARA','AORTA')).toEqual({correct:2,present:1,absent:2}));it('marca cada letra respeitando duplicatas',()=>expect(scoreMarks('ARARA','AORTA')).toEqual(['correct','present','absent','absent','correct']));it('fixa o primeiro puzzle no epoch',()=>expect(dayNumber('2026-07-18')).toBe(1));it('usa a mesma lista para palpites e respostas',()=>{expect(WORDS).toHaveLength(1445);expect(ANSWERS).toHaveLength(WORDS.length);expect(ANSWERS.map(answer=>answer.key)).toEqual(WORDS);expect(isWord('TERMO')).toBe(true);expect(isWord('BRAÇO')).toBe(true);expect(isWord('AMIGO')).toBe(true);expect(isWord('VASCO')).toBe(true);expect(isWord('MENGO')).toBe(true)});it('nunca entrega a resposta como dica',()=>expect(hintFor('BRACO',[],'')).not.toBe('BRACO'));it('mantém partidas infinitas fora das estatísticas diárias',()=>{const daily={version:2 as const,date:'2026-07-18',archive:false,draft:'',attempts:[],status:'won' as const,hintUsed:false,updatedAt:''},infinite={...daily,infinite:true,puzzle:42};expect(statistics([daily,infinite],'2026-07-18').played).toBe(1)});it('conta vitória com dica como derrota estatística',()=>{const game={version:2 as const,date:'2026-07-18',archive:false,draft:'',attempts:[],status:'won' as const,hintUsed:true,updatedAt:''};const stats=statistics([game],'2026-07-18');expect(stats.played).toBe(1);expect(stats.wins).toBe(0);expect(stats.current).toBe(0)})});
